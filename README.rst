unbiasKey
===============================

unbiasKey is a pipeline to call SNPs while reducing reference bias. To achieve that, we use bwa aln mapping with aDNA parameters, and use a stringent filtering during the variant calling steps.


